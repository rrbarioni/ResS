import static cucumber.runtime.groovy.EN.*

Given(~'^I want to change my residue generator address to "([^"]*)"') { String title ->
    //ResidueGenerator.find(address)
}

When (~'^I fill all the required fields correctly') { ->
   // ResidueGenerator.findAll()
}

And(~'^confirm my changes') { ->
   // ResidueGenerator.findAll()
}

Then(~'^the changes are properly saved by the system') { ->
    //ResidueGenerator.findAll()
}